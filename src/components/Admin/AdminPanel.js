// Thin wrapper to keep existing import paths stable while the AdminPanel is refactored.
// NOTE: Do NOT add patient-side cancel/reschedule features here.
import AdminPanelView from './AdminPanelView';

export default AdminPanelView;
